<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:hc="https://handyorg.github.io/handycontrol"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:helpers="clr-namespace:GeoChemistryNexus.Helpers"
                    xmlns:lg="clr-namespace:GeoChemistryNexus.Services"
                    xmlns:conv="clr-namespace:GeoChemistryNexus.Converter"
                    xmlns:layerModels="clr-namespace:GeoChemistryNexus.ViewModels">

    <conv:ListBoxItemCornerRadiusConverter x:Key="ListBoxItemCornerRadiusConverter"/>

    <Style x:Key="LayerIconContainerStyle" TargetType="Border">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="AxisChildIconTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource AxisIconForeground}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="LayerVisibilityCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#6E6E73"/>
        <Setter Property="ToolTip" Value="显示/隐藏"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="Root"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <Grid Width="17"
                              Height="17"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center">
                            <Path x:Name="EyeOutline"
                                  Data="M 1.8 8.5 C 1.8 8.5 4.8 3.8 8.5 3.8 C 12.2 3.8 15.2 8.5 15.2 8.5 C 15.2 8.5 12.2 13.2 8.5 13.2 C 4.8 13.2 1.8 8.5 1.8 8.5 Z"
                                  Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="1.7"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Fill="Transparent"/>
                            <Ellipse x:Name="EyePupil"
                                     Width="4.8"
                                     Height="4.8"
                                     Stroke="{TemplateBinding Foreground}"
                                     StrokeThickness="1.7"
                                     Fill="Transparent"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                            <Path x:Name="EyeSlash"
                                  Data="M 3 3 L 14 14"
                                  Visibility="Collapsed"
                                  Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="1.8"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#11000000"/>
                            <Setter Property="Foreground" Value="#1D1D1F"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#1A000000"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Foreground" Value="#A8A8AE"/>
                            <Setter TargetName="EyeSlash" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="EyeOutline" Property="Opacity" Value="0.7"/>
                            <Setter TargetName="EyePupil" Property="Opacity" Value="0.7"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Foreground" Value="#6E6E73"/>
                            <Setter TargetName="Root" Property="Background" Value="#11000000"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.38"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="LayerGroupIconBackground" Color="#26000000"/>
    <SolidColorBrush x:Key="LayerGroupIconForeground" Color="#6E6E73"/>
    <SolidColorBrush x:Key="LineIconBackground" Color="#26FF9500"/>
    <SolidColorBrush x:Key="LineIconForeground" Color="#FF9500"/>
    <SolidColorBrush x:Key="TextIconBackground" Color="#2634C759"/>
    <SolidColorBrush x:Key="TextIconForeground" Color="#34C759"/>
    <SolidColorBrush x:Key="PolygonIconBackground" Color="#265856D6"/>
    <SolidColorBrush x:Key="PolygonIconForeground" Color="#5856D6"/>
    <SolidColorBrush x:Key="ArrowIconBackground" Color="#26FF2D55"/>
    <SolidColorBrush x:Key="ArrowIconForeground" Color="#FF2D55"/>
    <SolidColorBrush x:Key="FunctionIconBackground" Color="#2600C7BE"/>
    <SolidColorBrush x:Key="FunctionIconForeground" Color="#00AFA8"/>
    <SolidColorBrush x:Key="AxisIconBackground" Color="#260A84FF"/>
    <SolidColorBrush x:Key="AxisIconForeground" Color="#0A84FF"/>
    <SolidColorBrush x:Key="PointIconBackground" Color="#2630B0C7"/>
    <SolidColorBrush x:Key="PointIconForeground" Color="#1F8FA8"/>

    <HierarchicalDataTemplate x:Key="LayerTreeItemTemplate" ItemsSource="{Binding Children}">
        <Grid Margin="2,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="28"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0"
                      Style="{StaticResource LayerVisibilityCheckBoxStyle}"
                      IsChecked="{Binding IsVisible}"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Left"
                      Margin="0,0,4,0"/>

            <Grid Grid.Column="1"
                  Width="20"
                  Height="20"
                  Margin="0,0,8,0"
                  VerticalAlignment="Center">
                <Border x:Name="GroupIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource LayerGroupIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Rectangle Width="8.5"
                                       Height="2"
                                       RadiusX="1"
                                       RadiusY="1"
                                       Fill="{StaticResource LayerGroupIconForeground}"
                                       Canvas.Left="2.75"
                                       Canvas.Top="3"/>
                            <Rectangle Width="8.5"
                                       Height="2"
                                       RadiusX="1"
                                       RadiusY="1"
                                       Fill="{StaticResource LayerGroupIconForeground}"
                                       Canvas.Left="2.75"
                                       Canvas.Top="6"/>
                            <Rectangle Width="8.5"
                                       Height="2"
                                       RadiusX="1"
                                       RadiusY="1"
                                       Fill="{StaticResource LayerGroupIconForeground}"
                                       Canvas.Left="2.75"
                                       Canvas.Top="9"/>
                        </Canvas>
                    </Viewbox>
                </Border>

                <Border x:Name="LineIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource LineIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Line X1="3"
                                  Y1="11"
                                  X2="11"
                                  Y2="3"
                                  Stroke="{StaticResource LineIconForeground}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"/>
                        </Canvas>
                    </Viewbox>
                </Border>

                <Border x:Name="TextIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource TextIconBackground}"
                        Visibility="Collapsed">
                    <TextBlock Text="T"
                               Foreground="{StaticResource TextIconForeground}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               FontFamily="Segoe UI"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,-1,0,0"/>
                </Border>

                <Border x:Name="PolygonIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource PolygonIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Polygon Points="7,2 12,5.5 10.2,12 3.8,12 2,5.5"
                                     Stroke="{StaticResource PolygonIconForeground}"
                                     StrokeThickness="1.6"
                                     StrokeLineJoin="Round"
                                     Fill="#335856D6"/>
                        </Canvas>
                    </Viewbox>
                </Border>

                <Border x:Name="ArrowIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource ArrowIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Line X1="3"
                                  Y1="11"
                                  X2="10"
                                  Y2="4"
                                  Stroke="{StaticResource ArrowIconForeground}"
                                  StrokeThickness="1.9"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"/>
                            <Path Data="M 7.2 3.8 L 10.5 3.5 L 10.2 6.8"
                                  Stroke="{StaticResource ArrowIconForeground}"
                                  StrokeThickness="1.9"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Fill="Transparent"/>
                        </Canvas>
                    </Viewbox>
                </Border>

                <Border x:Name="FunctionIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource FunctionIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Path Data="M 2 10 C 4 3, 6 3, 7 7 C 8.2 11, 10 11, 12 4"
                                  Stroke="{StaticResource FunctionIconForeground}"
                                  StrokeThickness="1.8"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Fill="Transparent"/>
                        </Canvas>
                    </Viewbox>
                </Border>

                <Border x:Name="AxisIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource AxisIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Line X1="3"
                                  Y1="10.5"
                                  X2="11.5"
                                  Y2="10.5"
                                  Stroke="{StaticResource AxisIconForeground}"
                                  StrokeThickness="1.6"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"/>
                            <Line X1="3.5"
                                  Y1="11"
                                  X2="3.5"
                                  Y2="2.5"
                                  Stroke="{StaticResource AxisIconForeground}"
                                  StrokeThickness="1.6"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"/>
                            <Path Data="M 9.5 8.7 L 11.5 10.5 L 9.5 12.3"
                                  Stroke="{StaticResource AxisIconForeground}"
                                  StrokeThickness="1.6"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Fill="Transparent"/>
                            <Path Data="M 1.7 4.5 L 3.5 2.5 L 5.3 4.5"
                                  Stroke="{StaticResource AxisIconForeground}"
                                  StrokeThickness="1.6"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Fill="Transparent"/>
                        </Canvas>
                    </Viewbox>
                </Border>

                <Border x:Name="AxisXIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource AxisIconBackground}"
                        Visibility="Collapsed">
                    <TextBlock Text="X"
                               Style="{StaticResource AxisChildIconTextStyle}"/>
                </Border>

                <Border x:Name="AxisYIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource AxisIconBackground}"
                        Visibility="Collapsed">
                    <TextBlock Text="Y"
                               Style="{StaticResource AxisChildIconTextStyle}"/>
                </Border>

                <Border x:Name="AxisAIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource AxisIconBackground}"
                        Visibility="Collapsed">
                    <TextBlock Text="A"
                               Style="{StaticResource AxisChildIconTextStyle}"/>
                </Border>

                <Border x:Name="AxisBIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource AxisIconBackground}"
                        Visibility="Collapsed">
                    <TextBlock Text="B"
                               Style="{StaticResource AxisChildIconTextStyle}"/>
                </Border>

                <Border x:Name="AxisCIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource AxisIconBackground}"
                        Visibility="Collapsed">
                    <TextBlock Text="C"
                               Style="{StaticResource AxisChildIconTextStyle}"/>
                </Border>

                <Border x:Name="PointIcon"
                        Style="{StaticResource LayerIconContainerStyle}"
                        Background="{StaticResource PointIconBackground}"
                        Visibility="Collapsed">
                    <Viewbox Width="14" Height="14">
                        <Canvas Width="14" Height="14">
                            <Ellipse Width="5.5"
                                     Height="5.5"
                                     Fill="{StaticResource PointIconForeground}"
                                     Canvas.Left="4.25"
                                     Canvas.Top="4.25"/>
                            <Ellipse Width="9"
                                     Height="9"
                                     Stroke="{StaticResource PointIconForeground}"
                                     StrokeThickness="1.4"
                                     Fill="Transparent"
                                     Canvas.Left="2.5"
                                     Canvas.Top="2.5"/>
                        </Canvas>
                    </Viewbox>
                </Border>
            </Grid>

            <TextBlock Grid.Column="2"
                       Text="{Binding Name}"
                       ToolTip="{Binding Name}"
                       MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=TreeView}, Path=ActualWidth}"
                       TextWrapping="NoWrap"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontWeight" Value="Normal"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasChildren}" Value="True">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Border Grid.Column="3"
                    Margin="8,0,0,0"
                    MinWidth="18"
                    Height="18"
                    Padding="6,0"
                    VerticalAlignment="Center"
                    Background="#E5E5EA"
                    CornerRadius="999">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasChildren}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding ChildCount}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="#3A3A3C"
                           TextAlignment="Center"/>
            </Border>
        </Grid>

        <HierarchicalDataTemplate.Triggers>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Group}">
                <Setter TargetName="GroupIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Line}">
                <Setter TargetName="LineIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Text}">
                <Setter TargetName="TextIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Polygon}">
                <Setter TargetName="PolygonIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Arrow}">
                <Setter TargetName="ArrowIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Function}">
                <Setter TargetName="FunctionIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Axis}">
                <Setter TargetName="AxisIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.AxisX}">
                <Setter TargetName="AxisXIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.AxisY}">
                <Setter TargetName="AxisYIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.AxisA}">
                <Setter TargetName="AxisAIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.AxisB}">
                <Setter TargetName="AxisBIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.AxisC}">
                <Setter TargetName="AxisCIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconKind}" Value="{x:Static layerModels:LayerTreeIconKind.Point}">
                <Setter TargetName="PointIcon" Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </HierarchicalDataTemplate.Triggers>
    </HierarchicalDataTemplate>

    <Style x:Key="ModernRibbonButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Margin" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="ToolTipService.InitialShowDelay" Value="360"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F3F4F6"/>
                            <Setter Property="BorderBrush" Value="#D1D5DB"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#E5E7EB"/>
                            <Setter Property="BorderBrush" Value="#9CA3AF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RibbonLargeButton" TargetType="Button" BasedOn="{StaticResource ModernRibbonButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="2"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E5F3FF"/>
                <Setter Property="BorderBrush" Value="#CCE7FF"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#CCE7FF"/>
                <Setter Property="BorderBrush" Value="#99D1FF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RibbonSmallButton" TargetType="Button" BasedOn="{StaticResource ModernRibbonButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="2"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E5F3FF"/>
                <Setter Property="BorderBrush" Value="#CCE7FF"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#CCE7FF"/>
                <Setter Property="BorderBrush" Value="#99D1FF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RibbonSmallToggleButton" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="ToolTipService.InitialShowDelay" Value="360"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#E5F3FF"/>
                <Setter Property="BorderBrush" Value="#CCE7FF"/>
            </MultiTrigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#CCE7FF"/>
                <Setter Property="BorderBrush" Value="#99D1FF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RibbonSplitButton" TargetType="hc:SplitButton" BasedOn="{StaticResource SplitButtonBaseStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="hc:BorderElement.CornerRadius" Value="4"/>
        <Setter Property="Foreground" Value="#323130"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1.0" />
                <Setter Property="Background" Value="#E5F3FF"/>
                <Setter Property="BorderBrush" Value="#CCE7FF"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Opacity" Value="1.0" />
                <Setter Property="Background" Value="#CCE7FF"/>
                <Setter Property="BorderBrush" Value="#99D1FF"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RibbonGroupStyle" TargetType="GroupBox">
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="#6C757D"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ContentPresenter Grid.Row="0" Margin="{TemplateBinding Padding}"/>
                        <TextBlock Grid.Row="1" Text="{TemplateBinding Header}" 
                                   FontSize="{TemplateBinding FontSize}"
                                   Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,2,0,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TemplateCardStyle" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransformOrigin" Value="0.5, 0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="#0078D4"/>
                <Setter Property="Background" Value="White"/>
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1.05" 
                                             Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1.05" 
                                             Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1.0" 
                                             Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1.0" 
                                             Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- New Styles from MainPlotPage -->
    
    <Style x:Key="TabLikeRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Name="Border" Background="Transparent"
                               BorderThickness="1,1,1,0" CornerRadius="3,3,0,0"
                               Padding="16,6,16,8" Margin="1,0" Height="28"
                               VerticalAlignment="Bottom">
                        <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="White"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="#D4D4D4"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#E5F3FF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#323130"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Width" Value="16"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent" Width="16" Height="16">
                        <Path x:Name="Arrow"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TreeViewItem}}"
                          StrokeThickness="1.5"
                          SnapsToDevicePixels="True"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Data="M 0 0 L 4 4 L 0 8">
                            <Path.RenderTransform>
                                <RotateTransform Angle="0" CenterX="2" CenterY="4"/>
                            </Path.RenderTransform>
                        </Path>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Arrow"
                                                     Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                     To="90" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Arrow"
                                                     Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                     To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModernTreeViewItemStyle" TargetType="TreeViewItem">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <StackPanel>
                        <Border x:Name="Bd"
                        CornerRadius="4"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="8,7" 
                        SnapsToDevicePixels="true">
                            <i:Interaction.Behaviors>
                                <helpers:TreeViewItemExpandBehavior />
                            </i:Interaction.Behaviors>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ToggleButton x:Name="Expander"
                                      Grid.Column="0"
                                      Style="{StaticResource ExpandCollapseToggleStyle}"
                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Margin="0,0,5,0"
                                      VerticalAlignment="Center"/>

                                <ContentPresenter x:Name="PART_Header"
                                          Grid.Column="1"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          ContentSource="Header"/>
                            </Grid>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost" Margin="20,0,0,0"/>

                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger SourceName="Bd" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#C2C3C9"/>
                        </Trigger>
                        <!-- 使用 DataTrigger 将 ViewModel 的 IsSelected 属性绑定 -->
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#35383E"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsSelectionActive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd" Property="Background" Value="#35383E"/>
                            <Setter Property="Foreground" Value="White"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsSelectionActive" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd" Property="Background" Value="#35383E"/>
                            <Setter Property="Foreground" Value="White"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Style TreeViewItem -->
    <Style x:Key="ModernStyleTreeViewItemStyle" TargetType="TreeViewItem">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <StackPanel>
                        <Border x:Name="Bd"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="8,6" 
                                SnapsToDevicePixels="true">
                            <i:Interaction.Behaviors>
                                <helpers:TreeViewItemExpandBehavior />
                            </i:Interaction.Behaviors>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ToggleButton x:Name="Expander"
                                              Grid.Column="0"
                                              Style="{StaticResource ExpandCollapseToggleStyle}"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              Margin="0,0,6,0"
                                              VerticalAlignment="Center"/>

                                <ContentPresenter x:Name="PART_Header"
                                                  Grid.Column="1"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                  ContentSource="Header"/>
                            </Grid>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost" Margin="20,0,0,0"/>

                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        
                        <!-- Hover State -->
                        <Trigger SourceName="Bd" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#EAEAEA"/>
                        </Trigger>

                        <!-- Selected State (Active) -->
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#007AFF"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                        
                        <!-- Selected State (Inactive/LostFocus) -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsSelectionActive" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd" Property="Background" Value="#0078D4"/> <!-- Modern Selection Blue - 保持与活动选中相同 -->
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FontStyleToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#EBF4FF"/>
                            <Setter Property="BorderBrush" Value="#A6CFFF"/>
                            <Setter Property="Foreground" Value="#0069D9"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#DCEBFF"/>
                            <Setter Property="BorderBrush" Value="#80BDFF"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Modern Style Sidebar RadioButton -->
    <Style x:Key="ModernSidebarRadioButton" TargetType="RadioButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#333333"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,2"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#007AFF"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                             <Setter TargetName="Border" Property="Background" Value="#E5E5EA"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" Value="#0062CC"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Style Card -->
    <Style x:Key="ModernCardStyle" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E5E5E5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>

    <!-- Modern Style Group Title -->
    <Style x:Key="ModernGroupTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="#8E8E93"/>
        <Setter Property="Margin" Value="4,0,0,8"/>
    </Style>

    <!-- Modern Style Label -->
    <Style x:Key="ModernLabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- Segmented Control Item Style (ListBoxItem) -->
    <Style x:Key="SegmentedItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="#E0E0E0"
                            BorderThickness="0,0,1,0"
                            SnapsToDevicePixels="True">
                        <Border.CornerRadius>
                            <MultiBinding Converter="{StaticResource ListBoxItemCornerRadiusConverter}">
                                <Binding RelativeSource="{RelativeSource TemplatedParent}"/>
                                <Binding RelativeSource="{RelativeSource AncestorType=ListBox}"/>
                            </MultiBinding>
                        </Border.CornerRadius>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12,8"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#007AFF"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#E5E5E5"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" Value="#0062CC"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Segmented Control Style (ListBox) -->
    <Style x:Key="SegmentedControlStyle" TargetType="ListBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#007AFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            ClipToBounds="True">
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Button Style -->
    <Style x:Key="ModernButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#007AFF"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#0062CC"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#0051A8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#A0A0A0"/>
                            <Setter Property="Foreground" Value="#E0E0E0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Style (Minimal) -->
    <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxExtend}">
        <Setter Property="Background" Value="#F2F2F7"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="hc:BorderElement.CornerRadius" Value="8"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>
    
    <!-- Modern Icon Button Style (Small) -->
    <Style x:Key="ModernIconButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#F2F2F7"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#E5E5EA"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#D1D1D6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Modern Numeric UpDown Style -->
    <Style x:Key="ModernNumericUpDownStyle" TargetType="hc:NumericUpDown" BasedOn="{StaticResource NumericUpDownExtend}">
        <Setter Property="Background" Value="#F2F2F7"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="hc:BorderElement.CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,6"/>
    </Style>

    <!-- ModernSettingsGroupTitle -->
    <Style x:Key="ModernSettingsGroupTitle" TargetType="TextBlock" BasedOn="{StaticResource ModernGroupTitleStyle}">
         <Setter Property="Margin" Value="16,20,0,8"/>
    </Style>

    <!-- ModernSettingsCard -->
    <Style x:Key="ModernSettingsCard" TargetType="Border" BasedOn="{StaticResource ModernCardStyle}">
         <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <!-- ToggleButtonSwitch -->
    <Style x:Key="ToggleButtonSwitch" TargetType="{x:Type ToggleButton}">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Background" Value="#E9E9EA"/>
        <Setter Property="BorderBrush" Value="#E9E9EA"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid>
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="12" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"/>
                        <Ellipse x:Name="Ellipse" Fill="White" Width="20" Height="20" HorizontalAlignment="Left" Margin="2,0,0,0">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="2" ShadowDepth="1" Opacity="0.3" Direction="270" Color="Black"/>
                            </Ellipse.Effect>
                        </Ellipse>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#34C759"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="#34C759"/>
                            <Setter TargetName="Ellipse" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="Ellipse" Property="Margin" Value="0,0,2,0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBoxBaseStyle -->
    <Style x:Key="ComboBoxBaseStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E5E5E5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
        <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid Name="MainGrid" SnapsToDevicePixels="true">
                        <Popup Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" Placement="Bottom" Margin="1" IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                            <Border x:Name="DropDownBorder" Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}">
                                <ScrollViewer Name="DropDownScrollViewer">
                                    <Grid RenderOptions.ClearTypeHint="Enabled">
                                        <Canvas Height="0" Width="0" HorizontalAlignment="Left" VerticalAlignment="Top">
                                            <Rectangle Name="OpaqueRect" Height="{Binding ElementName=DropDownBorder, Path=ActualHeight}" Width="{Binding ElementName=DropDownBorder, Path=ActualWidth}" Fill="{Binding ElementName=DropDownBorder, Path=Background}" />
                                        </Canvas>
                                        <ItemsPresenter Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Grid>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                        <ToggleButton Name="ToggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Grid.ColumnSpan="2" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}">
                             <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition />
                                                <ColumnDefinition Width="20" />
                                            </Grid.ColumnDefinitions>
                                            <Path x:Name="Arrow" Grid.Column="1" Fill="#999" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L4,4 L8,0"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                             </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ButtonIcon -->
    <Style x:Key="ButtonIcon" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F0F0F0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

