<hc:Window x:Class="GeoChemistryNexus.Views.LanguageSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:lg="clr-namespace:GeoChemistryNexus.Helpers"
        mc:Ignorable="d"
        Title="Language Selection" 
        Height="200" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Name="PromptText" 
                   Margin="0,0,0,10" FontSize="14" 
                   Foreground="{DynamicResource PrimaryTextBrush}"/>

        <hc:ComboBox Name="LanguageComboBox" Grid.Row="1" Margin="0,0,0,20" 
                     HorizontalAlignment="Stretch"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{Binding [Cancel],Source={x:Static lg:LanguageService.Instance}}" 
                    Click="Cancel_Click" Margin="0,0,10,0" Width="80"/>
            <Button Content="{Binding [Confirm],Source={x:Static lg:LanguageService.Instance}}" 
                    Style="{StaticResource ButtonPrimary}" Click="Ok_Click" Width="80"/>
        </StackPanel>
    </Grid>
</hc:Window>