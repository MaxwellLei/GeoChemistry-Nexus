<hc:Window x:Class="GeoChemistryNexus.Views.AddLinkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="添加链接" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <hc:TextBox Name="TitleBox" hc:InfoElement.Title="名称" hc:InfoElement.Necessary="True" Margin="0,0,0,15"/>
        
        <hc:TextBox Name="UrlBox" hc:InfoElement.Title="链接 (URL)" hc:InfoElement.Necessary="True" Grid.Row="1" Margin="0,0,0,15"/>
        
        <hc:TextBox Name="DescBox" hc:InfoElement.Title="描述" Grid.Row="2" Margin="0,0,0,15"/>
        
        <hc:ComboBox Name="IconBox" hc:InfoElement.Title="图标" Grid.Row="3" Margin="0,0,0,15" SelectedValuePath="Code">
            <hc:ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Code}" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </hc:ComboBox.ItemTemplate>
        </hc:ComboBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Click="Cancel_Click" Margin="0,0,10,0"/>
            <Button Content="确定" Style="{StaticResource ButtonPrimary}" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</hc:Window>
